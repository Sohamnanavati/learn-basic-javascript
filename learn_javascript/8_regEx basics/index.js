// A forward slash / starts and ends a regular expression in JavaScript.
// /g → global search, finds all matches instead of stopping at the first one.
// /i → case-insensitive search (doesn’t matter if letters are uppercase or lowercase).
// [ng] → matches either 'n' or 'g' at that position.
// [^ng] → matches anything except 'n' or 'g'.
// [a-zA-Z] → matches any letter, uppercase or lowercase.
// [0-9] → matches a single digit from 0 to 9.
// [0-9]+ → matches one or more digits in a row.
// [0-9]{11} → matches exactly 11 digits.
// [0-9]{5,8} → matches between 5 and 8 digits.
// [0-9]{5,} → matches 5 or more digits (no upper limit).

// meta characters
// \d  means [0-9]
// \w [a-zA-Z0-9 _ ] underscore
// \s matches the spaces, tabs etc
// \t match the tab characters

// "+" => one or more
//  \ escapre characters
// [ ] character set
// [^] negate symbol

// special characters
// ? 0 or 1 optional
// . any character
// * 0 or more
// sequence ?*+.
// .+ any character atlest 1s

// ^ start and then $ end 
// | => this is or symbol
// (p|t) => this indicates that this character can be either p or t (pyre or tyre)

// we can use 
// let reg = new RegExp()
//  or just 
// let reg = / [0-9][a-zA-Z]/ig

// regex.test(field.value) this will give true or false is the text matches the regrex then it returns true or else false.
// regex.exec()	Returns an Iterator of results
// regex.test()	Returns true or false


// //reference => https://www.youtube.com/watch?v=r6I-Ahc0HB4&list=PL4cUxeGkcC9g6m_6Sld9Q4jzqdqHd2HiD
// //reference => https://www.w3schools.com/js/default.asp
// // reference => https://regex101.com/

// <=======================more to be done. these are just few of the things=======================================>

















